{
  "name": "smartflowsite-landing smartflow-store-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development tsx server/index.ts",
    "lint": "echo 'no lint configured'",
    "build": "echo 'no build step' && exit 0",
    "test": "echo 'no tests' && exit 0"
  },
  "dependencies": {
    "express-rate-limit": "^8.1.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}